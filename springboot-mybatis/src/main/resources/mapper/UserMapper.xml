<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fit.dao.UserDao">

    <select id="queryAllUsers" resultType="com.fit.bean.User">
        select * from tab_user
    </select>

    <select id="queryUsersByName" parameterType="list" resultType="com.fit.bean.User">
        select * from tab_user
        where name in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>


    <select id="querySthUsersByName" resultType="com.fit.bean.User">
        select ${columns} from tab_user
        where name in
        <foreach item="item" index="index" collection="names" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>


    <select id="queryUsersByMap" parameterType="map" resultType="com.fit.bean.User">
        select ${columns} from tab_user
        where name in
        <foreach item="item" index="index" collection="names" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>


</mapper>
